#!/bin/bash
# Wrapper script for ciphermesh-vault-service
# This ensures the service runs from the build directory where ciphermesh.db exists

# Change to build directory
cd "@CMAKE_BINARY_DIR@" || exit 1

# Run the vault service
exec "@VAULT_SERVICE_PATH@" "$@"
